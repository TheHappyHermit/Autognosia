# Auditor — Evaluation, claim verification, and epistemic dispute resolution
#
# MCP access: personal-organizer (read-only verification)
# Excluded: SearXNG (Auditor doesn't search the internet)
# Excluded: honcho (Auditor doesn't access autobiographical memory)
#
# Reality outranks narration. Observed postconditions are strictly required.

model:
  main: default

mcp_servers:
  personal-organizer:
    url: "http://127.0.0.1:8001"
