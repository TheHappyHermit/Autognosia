# Cron Job: Integrity Check

**Job ID:** eba297436736
**Run Time:** 2026-08-28 19:31:39
**Schedule:** 30 2 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Run integrity check: execute the integrity_check.py script from the autognosia repository to verify database integrity and schema for organizer.db and cortex.db.

## Response

**Integrity Check Complete** ✅

Both databases are **HEALTHY**:

---

### `organizer.db` (`~/.autognosia/personal-organizer/data/organizer.db`)
| Check | Result |
|-------|--------|
| Integrity (PRAGMA integrity_check) | **PASS** — `ok` |
| Foreign Keys (PRAGMA foreign_key_check) | **PASS** — 0 violations |
| Expected Tables | **PASS** — all present |

**Tables found:** `important_dates`, `intentions`, `projects`, `reminders`, `sqlite_sequence`, `subscriptions`, `tasks`, `waiting_states`

---

### `cortex.db` (`~/.hermes-cortex/cortex.db`)
| Check | Result |
|-------|--------|
| Integrity (PRAGMA integrity_check) | **PASS** — `ok` |
| Foreign Keys (PRAGMA foreign_key_check) | **PASS** — 0 violations |
| Tables | **8 tables** — all present |
| Indexes | **11 indexes** — all present |

**Tables:** `key_decisions`, `operations`, `prospective_log`, `reflections`, `routing_events`, `skill_events`, `sqlite_sequence`, `verification_checks`  
**Indexes:** `idx_decisions_timestamp`, `idx_ops_profile`, `idx_ops_result`, `idx_ops_timestamp`, `idx_reflections_applied`, `idx_reflections_type`, `idx_routing_session`, `idx_routing_to`, `idx_skill_name`, `idx_skill_session`, `idx_verif_operation`

---

The official `integrity_check.py` from `autognosia-clean` covers only `organizer.db` (as it was designed). `cortex.db` passed manual verification with identical checks — no schema drift, no FK violations, full index coverage.
