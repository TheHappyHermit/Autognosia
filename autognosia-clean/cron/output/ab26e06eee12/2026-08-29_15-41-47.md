# Cron Job: Weekday Prompt-Me

**Job ID:** ab26e06eee12
**Run Time:** 2026-08-29 15:41:47
**Schedule:** 0 9 * * 1-5

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Use the prompt-me skill to ask one targeted question. Review recent sessions for context about what's currently at stake. Pick the most impactful single question that helps the user — something they haven't considered, a weak spot in a plan, a missing piece of context. Deliver the question directly to the user via the configured messaging service.

## Response

Based on my review of recent sessions, the Hermes Cortex deployment is nearly complete — all systems verified (12/12 checks passing, 5/5 Docker containers healthy), auto-setup script created, INSTALL.md updated, and all changes pushed to the private repo. However, one clear gap remains: the **`retrieval-reflex` skill was never installed**, which teaches agents *when* to retrieve from the brain. Without it, agents discuss topics with rich brain pages without ever opening them, then answer generically about facts the brain already knew.

---

**Value Prop:** I'm trying to optimize future agent reliability — ensuring any agent working with the Cortex automatically retrieves relevant brain context instead of hallucinating from training data.

**Hypothesis:** You *do* want `retrieval-reflex` installed (since you said "Sure" when I noted the gap), but the gbrain integration install failed because the Hermes profile directory (`~/.hermes`) lacks a `.git` directory, which gbrain's installer requires.

Should I initialize a minimal git repo in `~/.hermes` to satisfy gbrain's installer, then install `retrieval-reflex` properly — or was the omission intentional and the skill should remain absent?
