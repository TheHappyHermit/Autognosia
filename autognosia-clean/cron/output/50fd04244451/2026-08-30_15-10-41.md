# Cron Job: Brain Sync

**Job ID:** 50fd04244451
**Run Time:** 2026-08-30 15:10:41
**Schedule:** every 5m

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Run brain_sync_cron.py to sync knowledge bases to brain-postgres: cd ~/autognosia-clean && .venv/bin/python scripts/brain_sync_cron.py

## Response

## Brain Sync Cron — TIMEOUT

**Status:** ❌ Timed out after 600s

### Current DB State

| Metric | Value |
|--------|-------|
| Pages | 82 |
| Embeddings | 811 |
| Sources synced | 1 (`active-wiki` only) |

### Source Status

| Source | Files on DB | Files on Disk | Status |
|--------|-------------|---------------|--------|
| active-wiki | 84 | 135 | Partial (51 new) |
| oracle-brain | 0 | 1,330 | ❌ Not started |
| exchange-research | 0 | 0 | — |

### What Happened

The sync started processing `active-wiki` (84 pages already in DB, ~51 new to embed) but timed out before reaching `oracle-brain` which has **1,330 files** — all new, all needing embedding. With Ollama embedding batches taking 5-10s each, 1,300+ files would require many thousands of seconds, far exceeding the 600s timeout.

### Recommendation

Oracle-brain alone needs ~1,300 file syncs. Options:
1. **Run sync per-source** — `brain_sync.py --source active-wiki` first, then oracle-brain separately
2. **Increase timeout** — but 1,300 files could take 30-60+ min
3. **Run oracle-brain sync manually** in background with no timeout cap
