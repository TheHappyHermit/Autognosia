# Cron Job: Brain Sync

**Job ID:** 50fd04244451
**Run Time:** 2026-08-30 16:58:22
**Schedule:** every 5m

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Run brain_sync_cron.py to sync knowledge bases to brain-postgres: cd ~/autognosia-clean && .venv/bin/python scripts/brain_sync_cron.py

## Response

## brain_sync_cron.py — TIMEOUT (Ollama unresponsive)

**Status:** ❌ Failed — sync timed out after 600s

**Root cause:** The Ollama embedding server at `http://10.1.1.10:11434` is completely unresponsive. Every embedding call is timing out — even batches of 1-2 texts fail after 60-80s each.

**What happened:**
- 130 .md files scanned from active-wiki
- First file's embedding batch started retrying — all 3 retries timed out
- Fallback splitting also failed (batches of 2, then single texts)
- After 12+ minutes of retry hell, the process was still on the first file
- The 600s cron timeout killed it

**DB state:** Unchanged — last successful sync was 2026-08-30 23:04:17 UTC (partial, active-wiki only: 130 files, 1122 chunks). oracle-brain was not reached this run.

**Files in scope:**
- active-wiki: 130 files (2.9 MB)
- oracle-brain: 1,321 files (27.8 MB) ← this is the heavy one
- exchange-research: 0 files

**Diagnosis:** The iGPU Ollama server (10.1.1.10:11434) that was switched to on 2026-08-30 is not responding. The brain_sync retry logic (3 retries + exponential backoff + batch splitting) compounds the problem — each failing batch can consume 5+ minutes, making the 600s cron timeout effectively unreachable.

**Recommendation:** Check the Ollama server health on 10.1.1.10:11434 (`systemctl status ollama` or `docker logs`). The sync will not complete until embedding is reachable again. Consider also reducing the retry count or adding a circuit-breaker that aborts after N consecutive failures rather than retrying every batch to death.
