Cronjob Response: Session Audit
(job_id: e67de44b4854)
-------------

# Knowledge Review Report — Aug 10–17, 2026

## 1. Major Decisions Not Saved to Wiki

### A. Hermes Cortex Architecture (Critical Gap)
The comprehensive architecture documented in the GitHub repo's README.md and INSTALL.md (sessions `20260812_071004` and `20260812_093503`) has **not been ingested into the active wiki**. The repo contains a full cognitive architecture specification — honcho, personal ops, oracle, GBrain, epistemic control, experience index, verification, action gates — but the active wiki has only 5 files (AGENTS.md, index, HOW-TO-USE, SCHEMA, Welcome.md). The Oracle brain has 107+ research domains but the active/current knowledge layer is essentially empty.

**Recommendation:** Ingest the README.md and INSTALL.md from `openclaw434/hermes-cortex` into the active wiki under `projects/hermes-cortex/`. This is the user's primary architectural document and should be in hot retrievable knowledge, not just in a GitHub repo.

### B. User Preferences Discovered in Aug 16 Session But Not Persisted
The massive Aug 16 session (`20260816_140606_20529d23`, 362 messages) established several important preferences:

- **Cross-OS compatibility priority** — scripts must work on macOS, WSL, and Linux; no hardcoded `/home/josh434/` paths
- **Zero-config setup mandate** — `auto_setup.sh` must work on first clone with no manual editing
- **GBrain runs via Bun/CLI PGLite** — not Docker; the Docker compose file is documentation-only
- **SearXNG dual-path** — user uses external `https://search.wineandgecko.com/` via skill, but repo must retain Docker fallback for general users
- **Graphify graceful handling** — when CLI is missing, scripts must exit 0, not crash
- **Verify stack passes 12/12** after path correction (`PERSONAL_AGENT` → `CORTEX_HOME`)

**Recommendation:** Create `personal/cortex-deployment-preferences.md` in the active wiki documenting these. They were decided in a single massive session and lost to context compaction.

### C. Cron Job Architecture (30 Jobs)
The full cron job topology was established in the Aug 16 session:

| Category | Jobs | Schedule |
|----------|------|----------|
| Newsletters | Morning (6 AM), Evening (9 PM) | Daily |
| Health/Monitoring | Cortex Health Check (every 30m), Daily Health Check (08:00), Integrity Check (02:30) | Continuous |
| Backups | Config Backup (01:00), Database Backup (02:00), Daily Backup (03:00) | Daily |
| Wiki/Memory | Wiki Lint Daily (04:00), Wiki Lint Weekly Deep (Sun 03:00), Memory Consolidation Daily (04:00), Full Cascade (Sun 04:00) | Daily/Sun |
| Knowledge | Oracle Knowledge Expansion (02:45), Oracle Index Rebuild (03:30), GBrain Sync (every 1h), Research Exchange Sync (Wed 03:00) | Continuous/Wed |
| Operations | Personal State Reminders (every 15m), Experience Capture (every 30m), Graphify Refresh (Sun 04:00) | Continuous/Sun |
| Briefings/Views | Daily Briefing (07:00), View Generation (05:00), Intention Check (08:00) | Daily |
| Reviews | Session Audit (Mon 06:00), Weekday Prompt-Me (Mon-Fri 09:00), Weekly Review (Sun 09:00), Persona Audit (15th 08:00), Monthly Systems Review (1st 10:00), Workflow Suggestion (1st 11:00) | Periodic |

**Recommendation:** Document the cron topology in `system/cron-architecture.md`. This is the operational nervous system and should be wiki-documented.

### D. GBrain PGLite Deployment Details
Session Aug 16 confirmed:
- GBrain data location: `~/.gbrain/brain.pglite/`
- Config: `~/.gbrain/config.json`
- Health check: `gbrain doctor` (returns 0 when healthy)
- The `docker-compose.gbrain.yml` in the repo is **broken** and should be removed (it was)
- SearXNG external URL: `https://search.wineandgecko.com/`
- llama.cpp server: `10.1.1.10:8080`

**Recommendation:** Document in `system/gbrain-deployment.md`. These are concrete infrastructure facts that will be needed on rebuild.

---

## 2. Troubleshooting Results Worth Preserving

### A. Verified Fixes (Aug 16 Session)
These were tested and confirmed working in session `20260816_140606_20529d23`:

| Issue | Fix | Status |
|-------|-----|--------|
| `verify_stack.py` Personal Ops check | Fixed directory path `PERSONAL_AGENT` → `CORTEX_HOME` | ✅ Verified |
| bash brace-expansion `{1..N}` | Changed to POSIX `while [ $count -lt N ]` loops | ✅ macOS/WSL compatible |
| `bun` PATH resolution | Added fallback detection: `~/.bun/bin`, `/usr/local/bin`, `/opt/homebrew/bin` | ✅ Cross-OS |
| `health_check.py` GBrain detection | Added `gbrain doctor` fallback alongside HTTP port check | ⚠️ **Still exits 1** — unresolved |
| `init_graphify.py` / `refresh_graphify.py` | Guarded behind `graphify_available` check | ✅ Graceful skip |
| `auto_setup.sh` first-run | Created 356-line zero-config setup orchestrator | ⚠️ **Not end-to-end tested** |

### B. Unresolved Issue — `health_check.py` Still Failing
The health check script exits 1 because the GBrain PGLite detection patch was applied but the subprocess PATH resolution didn't include `~/.bun/bin`. Multiple patch attempts in the Aug 16 session did not fully resolve this.

**Recommendation:** This should be fixed. A daily health check that reports "unhealthy" for a healthy system degrades trust in the monitoring.

---

## 3. Wiki Maintenance Issues

### A. Empty Ingestion Log
`~/.hermes-cortex/active-wiki/.meta/ingestion-log.md` is **empty**. No ingestions have been logged. This violates the wiki-ingestion skill's requirement to log every ingestion.

**Recommendation:** Start logging. The knowledge that was created on Aug 12–16 should be backlogged.

### B. Empty Memory Archives
Both `system/memory-archive/decisions.md` and `system/memory-archive/preferences.md` say "*Nothing archived yet.*" — decisions and preferences that were decided in sessions Aug 12 and Aug 16 are not being archived.

**Recommendation:** Begin archiving. The preferences and decisions documented in section 1 above should move here after they've been in hot memory for a period.

### C. Archive Directory Bloat
The `_archive/agent_zero_kb_import/` directory contains 80+ files from an old Agent Zero knowledge base import. These are legacy artifacts that are unlikely to be relevant to current work.

**Recommendation:** Review and archive/remove these files. They clutter the wiki and may confuse retrieval.

### D. Skeleton Directories
Directories `personal-finance/`, `purchases/`, and `projects/` contain only rule files (`AGENTS.md`, `_template/project-template.md`) but no actual content pages. The wiki is structured for these domains but nothing has been populated.

**Recommendation:** Either populate them with relevant content or remove the directory structure to avoid false signals about what exists.

---

## 4. Summary of Recommendations

| Priority | Action | Reason |
|----------|--------|--------|
| **High** | Ingest Hermes Cortex README.md/INSTALL.md into active wiki | Primary architectural knowledge not in hot retrievable layer |
| **High** | Fix `health_check.py` GBrain detection | Daily health check reports false negatives |
| **High** | Create ingestion log entries for Aug 12–16 work | Wiki maintenance standard violated |
| **Medium** | Document cron topology in wiki | 30-job operational nervous system undocumented |
| **Medium** | Document GBrain PGLite deployment details | Infrastructure facts lost to context compaction |
| **Medium** | Document user deployment preferences | Cross-OS, zero-config, SearXNG dual-path decisions |
| **Medium** | Clean up `_archive/agent_zero_kb_import/` | 80+ legacy files cluttering wiki |
| **Low** | Populate or remove skeleton directories (`personal-finance/`, `purchases/`, `projects/`) | False signals about existing knowledge |
| **Low** | Archive preferences/decisions from Aug 12–16 to memory archive | Following wiki lifecycle rules |

---

## 5. Cron Health Status

- **30/30 cron jobs running** — all documented jobs accounted for
- **Newsletter jobs** (morning/evening) — generating successfully
- **Personal State Reminders** (every 15m) — running
- **Cortex Health Check** (every 30m) — running, but `health_check.py` exits 1 (false negative)
- **Experience Capture** (every 30m) — running
- All other scheduled jobs (hourly, daily, weekly, monthly) — created and active

No cron jobs show delivery errors in the visible data.

To stop or manage this job, send me a new message (e.g. "stop reminder Session Audit").