Cronjob Response: Session Audit
(job_id: e67de44b4854)
-------------

Now I have all the data needed. Let me compile the report.

**Cron Job Knowledge Review — Last 7 Days (Aug 17–24, 2026)**

---

## 1. User Decisions Worth Saving to Wiki

### A. Research Lanes A/B Paused (Aug 23)
**Decision:** User explicitly paused Frontier Research Lane A (`a5e01552e0f7`) and Lane B (`1d578cbabe84`) due to V100 GPU overload causing 90s+ inference latency. They asked to be notified when to resume (graphify completion would be a good cue).

**Current state:** Still paused as of Aug 24. Graphify finished on Aug 23. This decision should be recorded in the wiki so future runs don't accidentally unpause them.

**Recommendation:** Add to `wiki/system/core-preferences.md` or create `system/research-lanes-status.md`:
> "Research Lanes A & B paused Aug 23 due to V100 contention. Resume when graphify completes and no competing LLM workloads. User prefers manual resumption trigger."

### B. Model/Provider Drift (Aug 17–23)
**Decision:** Global inference config changed from `custom` (llamaCPP @10.1.1.10:8080) to `nous` (stealth/ox-alpha). Jobs pinned to the old config survived; unpinned jobs were silently skipped via `drift_skip`.

**Current state:** Oracle Knowledge Expansion batches 1–3 (pinned to llamaCPP) run fine. Batches 0 and 4 (unpinned) fail with `drift_skip`. Graphify Progress Monitor (`5f2468a5d037`) also `drift_skip`.

**Recommendation:** Update `system/model-config.md` — it currently says:
- Cortex is the default provider (now retired)
- Primary fallback is `nvidia/nemotron-3-ultra-550b-a55b:free` (may be outdated)

The page needs updating to reflect:
- Current default is `nous/stealth/ox-alpha`
- llamaCPP remains in use for cron jobs that are pinned
- Oracle batches should be pinned to `llamaCPP` model to avoid future drift

---

## 2. Troubleshooting Results Worth Preserving

### A. V100 Overload Root Cause (Aug 23)
**Finding:** A "reply OK" test took 94s. The root cause was simultaneous workloads piling onto one llama.cpp instance: manual graphify ingest + Honcho deriver re-derivations + research lane attempts. Ollama (embeddings) was fine and separate.

**Resolution:** Paused Research Lanes A/B. Graphify ran overnight, finished Aug 23–24.

**Should be recorded in wiki** as `system/v100-contention-pattern.md` or added to the existing graphify-policy page:
> "V100 contention pattern: graphify extract + Honcho deriver + research lanes simultaneously causes >90s inference latency. Solution: stagger workloads — run graphify in its own overnight window (01:00–05:00) separate from live traffic."

### B. cortex.db Retirement Verification (Aug 23–24)
**Result:** `cortex.db` confirmed absent. `autognosia.db` is the sole event log (13.6MB, last updated 02:30 Aug 24). Integrity check on `organizer.db` passes (all checks: PASS).

**Already captured** in memory notes — no wiki update needed.

### C. Embed Shim Removal (Aug 23)
**Result:** `docker/embed_shim.py` deleted, `embed-shim` service block removed from compose config, all references swept. Pushed to GitHub (commit `4a4632c`).

**Already captured** in memory notes — no wiki update needed.

---

## 3. Active Cron Job Issues Requiring Attention

| Job | Error | Status | Fix Needed |
|-----|-------|--------|------------|
| **Database Backup** (762c05cb9451) | `TERMINAL_CWD read lock timeout after 660s` | 1 failure today | Stagger schedule or remove workdir — same issue as Wiki Lint & Memory Consolidation |
| **Oracle Index Update** (3a11d29b7c57) | Script not found at `.hermes/scripts/` | 4 failures in a row | Script lives at `.autognosia/scripts/oracle_index_update.py`. Cron job path is wrong. Either copy to `.hermes/scripts/` or update cron workdir. |
| **Wiki Lint Daily** (1752062c0ea4) | `TERMINAL_CWD read lock timeout` | 1 failure today | Same lock contention as Database Backup |
| **Memory Consolidation Daily** (e3f5222b463f) | `TERMINAL_CWD read lock timeout` | 1 failure today | Same lock contention |
| **View Generation** (14faed0cbba7) | `TERMINAL_CWD read lock timeout` | 1 failure today | Same lock contention |
| **Oracle Knowledge Expansion batch 0** (1983f8c1f638) | `timed out after 240s` | 1 failure today | Ran during peak graphify contention window |
| **Oracle Knowledge Expansion batch 4** (7042e4e8d59e) | `timed out after 240s` | 3 in a row | Same issue — lock contention from graphify |
| **Graphify Refresh** (703dd7f07c60) | "Main Graph not initialized" | 1 failure (weekly) | Graphify extract ran manually but may not have completed full graph initialization |
| **Daily Health Check** (2f8c625a2cc2) | Schema Conformance FAIL | 1 failure (yesterday) | `verify_schema_conformance.py` failing — needs investigation |
| **Graphify Progress Monitor** (5f2468a5d037) | `drift_skip` | 2 in a row | Unpinned cron job — needs `hermes cron edit` to pin model |

**Root cause cluster:** 5 jobs share the `TERMINAL_CWD read lock` timeout — this is a cron scheduler contention issue (#79768). Jobs with `workdir` set to `.autognosia/scripts/` are competing for the same lock.

---

## 4. Wiki Maintenance Findings

**Orphan pages (20 found):** Pages with no `Source:` header. This is a recurring issue — these pages likely predate the Source tracking convention. The list includes all index pages, AGENTS.md files, and system reference pages. Not urgent but worth cleaning up on a maintenance pass.

**Stale pages:** None detected (0 pages older than 60 days without Source headers).

**Graphify report files** in `active-wiki/graphify-out/` (3 files from Aug 20–23) are data artifacts, not wiki pages — they don't need Source headers.

---

## 5. Recommendations Summary

**Priority 1 — Fix the lock contention (5 jobs failing):**
The `TERMINAL_CWD read lock` errors suggest multiple cron jobs share the same workdir and the scheduler serializes them. Options:
- Remove `workdir` from jobs that don't need it (Database Backup, Wiki Lint Daily, Memory Consolidation, View Generation)
- Or stagger their schedules to avoid overlap

**Priority 2 — Fix Oracle Index Update (4 failures):**
The script exists at `.autognosia/scripts/oracle_index_update.py` but the cron job looks for it at `.hermes/scripts/oracle_index_update.py`. Copy it or fix the path.

**Priority 3 — Pin remaining drifted cron jobs:**
Graphify Progress Monitor (`5f2468a5d037`) needs `hermes cron edit 5f2468a5d037 --provider nous --model stealth/ox-alpha` (or pin back to the old config).

**Priority 4 — Record user decisions in wiki:**
- Research Lanes A/B pause decision
- V100 contention pattern and resolution

**Priority 5 — Investigate Daily Health Check failure:**
Schema Conformance is failing — this is the one health check item that's broken (13/14 pass). Verify `verify_schema_conformance.py` runs correctly.

**Low priority — Wiki cleanup:**
Add `Source:` headers to 20 orphan pages when doing the next wiki maintenance pass.

---

**Overall health:** 15 of 26 cron jobs running healthy today. The main risk is the lock contention cascade and the 3 failed Oracle Knowledge Expansion batches (batch 0 timeout, batch 4 timeout, batch 4 is 3-in-a-row). Oracle batches 1–3 ran successfully. Personal State is clean (1 subscription review intention, no due reminders). Database integrity is passing.

To stop or manage this job, send me a new message (e.g. "stop reminder Session Audit").