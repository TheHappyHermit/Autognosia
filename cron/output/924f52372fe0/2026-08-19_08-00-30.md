# Cron Job: Intention Check

**Job ID:** 924f52372fe0
**Run Time:** 2026-08-19 08:00:30
**Schedule:** 0 8 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Check prospective memory for dormant intentions in organizer.db. Review cues (dates, events, context changes) and promote any that are now actionable. Archive expired intentions. Use the prompt-me and organizer-state skills.

## Response

The database query is pending user approval. Since this is a cron job with no user present, I cannot execute the database reads needed to complete this task.

Here's the situation:

**What I can do:** I've loaded the `prospective-memory` and `prompt-me` skills and understand the workflow — review dormant intentions, check cues (dates, events, context changes), promote actionable ones, and archive expired ones.

**What I can't do:** The `organizer.db` file at `~/.hermes/memory/organizer.db` requires database queries (sqlite3 or Python sqlite3 module) to inspect intentions, triggers, and states. Both `execute_code` (Python) and `terminal` (sqlite3) are blocked in cron mode because they require user approval to execute — and no user is present to approve.

**To fix this, you have two options:**

1. **Set `approvals.cron_mode: approve`** in your Hermes config for this cron profile, so terminal/Python execution is auto-approved during cron runs. Then I can query the database and complete the prospective memory review.

2. **Manually run** the check yourself and paste the output here. A simple command would be:
   ```
   sqlite3 ~/.hermes/memory/organizer.db "SELECT * FROM intentions;"
   ```
   (Or whatever tables/schemas your organizer uses.)

Until approval is configured, I'm unable to read `organizer.db` and therefore cannot review or promote any dormant intentions.
