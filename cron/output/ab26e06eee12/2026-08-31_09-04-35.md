# Cron Job: Weekday Prompt-Me

**Job ID:** ab26e06eee12
**Run Time:** 2026-08-31 09:04:35
**Schedule:** 0 9 * * 1-5

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Use the prompt-me skill to ask one targeted question. Review recent sessions for context about what's currently at stake. Pick the most impactful single question that helps the user — something they haven't considered, a weak spot in a plan, a missing piece of context. Deliver the question directly to the user via the configured messaging service.

## Response

**The Value Prop:** I need to optimize the terminal execution environment so cron jobs and automated sessions don't waste cycles fighting a broken working directory.

**The Hypothesis:** There's a terminal session-level working directory set to `/tmp/oc-dashboard-20260831T030225Z` (a deleted OpenCode workspace from Aug 30). Every cron job session starts with this as the working directory, causing all terminal commands to fail with exit 126 until `workdir=/home/josh434` is explicitly passed. This is a persistent environment pollution from an old OpenCode session — it's likely in the terminal daemon config or a profile script that sets `PWD` on shell startup. The fix would be either: (a) clean up the terminal session config so it defaults to `$HOME`, or (b) set `workdir=/home/josh434` in every cron job's terminal call. I'm assuming the right fix is (a) — a one-time environment cleanup — but I could be wrong. Should I dig into what's setting this stale working directory, or is there a different root cause I'm missing?
