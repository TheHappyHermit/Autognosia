# Cron Job: Integrity Check

**Job ID:** eba297436736
**Run Time:** 2026-08-29 02:31:46
**Schedule:** 30 2 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Run integrity check: execute the integrity_check.py script from the autognosia repository to verify database integrity and schema for organizer.db and cortex.db.

## Response

**Integrity Check Report — 2026-08-29T09:31Z**

## organizer.db
**Status: HEALTHY**

| Check | Result |
|---|---|
| Integrity | PASS |
| Foreign Keys | PASS |
| Tables | PASS |

**Active path:** `~/.autognosia/personal-organizer/data/organizer.db` (94,208 bytes, modified Aug 28 15:36)

**Tables:**
- `tasks`: 4 rows
- `projects`: 1 row
- `subscriptions`: 0 rows
- `important_dates`: 1 row
- `intentions`: 2 rows
- `waiting_states`: 1 row
- `reminders`: 1 row (not in expected list but present)

---

## cortex.db
**Status: WARN**

| Check | Result |
|---|---|
| Integrity | PASS |
| Foreign Keys | PASS |
| Tables | 8 tables found |
| Indexes | 11 indexes found |
| Schema completeness | WARN |

**Active path:** `~/.hermes-cortex/cortex.db` (532,480 bytes, modified Aug 19 00:30)

**Tables:**

| Table | Rows |
|---|---|
| operations | 978 |
| routing_events | 38 |
| reflections | 3 |
| key_decisions | 0 |
| prospective_log | 0 |
| skill_events | 0 |
| verification_checks | 0 |
| sqlite_sequence | 3 |

**Missing from expected schema:** `entities`, `relationships`, `memories`, `references`, `sources`, `embeddings`, `cortex_graphs`, `cortex_index`

---

## ⚠️ Issue: cortex.db backup is stale but has MORE data

The backup at `~/.autognosia/backups/cortex.db` (3.9MB, Aug 21) contains **more rows than the active DB**:

| Table | Active | Backup |
|---|---|---|
| operations | 978 | **7,208** |
| routing_events | 38 | **1,410** |
| reflections | 3 | **37** |
| key_decisions | 0 | 0 |

This suggests the active `~/.hermes-cortex/cortex.db` was reset/truncated sometime after Aug 21, while the backup retained the older, larger dataset. The active DB's last modification date (Aug 19) is **before** the backup date (Aug 21), confirming the active DB is older than the backup despite having fewer rows.

The backup script (`backup_databases.py`) only backs up `organizer.db` — `cortex.db` and `autognosia.db` are excluded by design (confirmed in Aug 27 cron log).
