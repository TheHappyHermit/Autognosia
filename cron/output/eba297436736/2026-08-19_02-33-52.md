# Cron Job: Integrity Check

**Job ID:** eba297436736
**Run Time:** 2026-08-19 02:33:52
**Schedule:** 30 2 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Run integrity check: execute the integrity_check.py script from the autognosia repository to verify database integrity and schema for organizer.db and cortex.db.

## Response

## Integrity Check Report — 2026-08-19

### organizer.db
| Instance | Integrity | Foreign Keys | Status |
|---|---|---|---|
| `~/.autognosia/personal-organizer/data/organizer.db` | **PASS** | **PASS** | **HEALTHY** |
| `~/.autognosia/backups/organizer.db` | **PASS** | **PASS** | **HEALTHY** (empty snapshot) |

**Notes:**
- Live DB: 8 tables, 9 rows total (tasks: 2, plus 1 each in projects/subscriptions/important_dates/intentions/waiting_states/reminders)
- Backup: 7 tables, 0 rows — older snapshot missing `reminders` table (added later)

### cortex.db
| Instance | Size | Integrity | Foreign Keys | Status |
|---|---|---|---|---|
| `~/.autognosia/cortex.db` | 456 KB | **PASS** | **PASS** | **HEALTHY** (818 ops) |
| `~/.hermes-cortex/cortex.db` | 520 KB | **PASS** | **PASS** | **HEALTHY** (978 ops) |
| `~/.hermes/cortex.db` | 0 bytes | **PASS** | **PASS** | **Empty** (0 tables) |

**Notes:**
- Both active cortex.db instances share identical 8-table schema (operations, verification_checks, routing_events, skill_events, reflections, key_decisions, prospective_log, sqlite_sequence)
- `.hermes/cortex.db` is a 0-byte placeholder — no schema loaded
- `.hermes-cortex/cortex.db` has slightly more operations (978 vs 818)

### About `integrity_check.py`

The script at `~/.hermes/scripts/integrity_check.py` only checks `organizer.db`. I created `~/.hermes/scripts/check_cortex_dbs.py` to also verify all cortex.db instances.

**Ad-hoc verification passed** — the check script correctly identifies all 5 databases, reports integrity/FK status, and flags the empty `.hermes/cortex.db` placeholder.
